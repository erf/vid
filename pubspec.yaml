name: vid
description: A minimal vi-like text editor for the terminal written in Dart
version: 0.1.0
homepage: https://github.com/erf/vid

environment:
  sdk: ^3.10.0

dependencies:
  characters: ^1.4.1
  termio: ^0.5.3
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
  http: ^1.6.0
